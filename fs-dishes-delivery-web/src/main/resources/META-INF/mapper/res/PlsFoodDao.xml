<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fs.dishes.module.res.dao.PlsFoodDao">
    <select id="queryList" parameterType="map" resultType="com.fs.dishes.module.res.entity.PlsFood">
        select * from pls_food
        <where>
            1=1
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="name != null and name !='' ">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="speciesId != null">
                AND species_id = #{speciesId}
            </if>
        </where>
    </select>

    <select id="exists" parameterType="PlsFood" resultType="java.lang.Boolean">
        select count(1) from pls_food
        <where>
            status = 1
            <if test="id != null">
                AND id &lt;&gt; #{id}
            </if>
            <if test="name != null and name !='' ">
                AND name = #{name}
            </if>
        </where>
    </select>
</mapper>